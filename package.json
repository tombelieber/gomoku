{
  "name": "open-gomoku",
  "private": true,
  "version": "0.0.1",
  "description": "Gomoku (五子棋) — State-of-the-art AI game built in Rust + WASM. Play online or learn how modern AI works.",
  "scripts": {
    "dev": "cd web && bun dev",
    "build:engine": "wasm-pack build engine/ --target web --out-dir ../web/src/wasm/engine",
    "build:web": "cd web && bun run build",
    "build": "bun run build:engine && bun run build:web",
    "deploy": "bun run build && cd web && bunx wrangler pages deploy dist --project-name gomoku"
  }
}
