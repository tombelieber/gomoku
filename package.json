{
  "name": "open-gomoku",
  "private": true,
  "version": "1.4.1",
  "description": "Gomoku (五子棋) — State-of-the-art AI game built in Rust + WASM. Play online or learn how modern AI works.",
  "scripts": {
    "dev": "cd web && bun dev",
    "build:engine": "wasm-pack build engine/ --target web --out-dir ../web/src/wasm/engine",
    "build:web": "cd web && bun run build",
    "build": "bun run build:engine && bun run build:web",
    "deploy": "bun scripts/version-bump.ts && bun run build && cd web && bunx wrangler pages deploy dist --project-name open-gomoku",
    "demo:preview": "bun run scripts/record-demo.ts --preview",
    "demo": "bun run scripts/record-demo.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "lefthook": "^2.1.3",
    "playwright": "^1.58.2"
  }
}
